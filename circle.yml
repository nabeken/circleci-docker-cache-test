machine:
  services:
    - docker

dependencies:
  override:
    - "./docker-pull.sh pull"
    - "./docker-pull.sh serial"
    - "./docker-pull.sh clear"
    - "./docker-pull.sh parallel"

test:
  override:
    - /bin/true
    - docker version
    - docker info
    - docker images
