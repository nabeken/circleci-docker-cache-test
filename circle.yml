machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - "ls -alh ~/docker"
    - "rm -rf ~/docker/*"

test:
  override:
    - /bin/true
    - docker version
    - docker info
