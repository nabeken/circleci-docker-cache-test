machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker2"
  override:
    - "mkdir -p ~/docker2"
    - "ls -alh ~/docker2"
    - "./docker-pull.sh"
    - "ls -alh ~/docker2"

test:
  override:
    - /bin/true
    - docker version
    - docker info
    - docker images
