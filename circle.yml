machine:
  services:
    - docker

dependencies:
  override:
    - "./docker-pull.sh pull"
    - "./docker-pull.sh serial"
    - "./docker-pull.sh clear"
    - "./docker-pull.sh parallel"
    - "./docker-pull.sh clear"
    - "./docker-pull.sh serial"

test:
  override:
    - /bin/true
    - docker version
    - docker info
    - docker images
